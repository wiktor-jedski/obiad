# Task List

Valid Task statuses: OPEN, PREPARED, PASSED
ID: Use growing unique integers
Verification Criteria: name a `Run` command, state observable evidence after `Pass:`, and map ordered phase-gate sentences with markers such as `P01-G1`.

## Phase 1 — Food Catalog schema

| ID  | Architecture Component                                                             | Status   | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Depends On (ID)                                                 | Verification Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | ARCH-007, ARCH-013 | PASSED | Create `backend/go.mod`, an embedded transactional versioned-migration runner, `backend/cmd/dbsetup`, and an initial SQL migration under `backend/internal/repository/sql/` that creates Food Object identity, constrained localized-name JSONB, and Physical State without seed rows, following [ARCH-007](../architecture/architecture.md#arch-007--database-setup-module), [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), [ADR 0001](../architecture/0001-localized-names-jsonb.md#store-localized-names-in-food-object-jsonb), [REQ-005](../requirements/requirements.md#req-005--stable-food-object-identity), [REQ-006](../requirements/requirements.md#req-006--required-localized-names), and [the glossary](../../CONTEXT.md#food-objects). | | Run `go run ./cmd/dbsetup` against an empty disposable PostgreSQL database and run `go test ./...` from `backend/`. Pass: integration tests `TestDBSetupAppliesVersionedMigrations`, `TestFoodObjectIdentityAndLocalizedNames`, and `TestFoodObjectPhysicalState` show one migration transaction, positive IDs, object JSONB with nonempty string `en` and `pl` values, rejection of missing, empty, or wrong-type names, nonpositive IDs, and unsupported states, one ID for both localized names, and zero Food Object rows (`P01-G1`, `P01-G3`, `P01-G4`, `P01-G5`). |
| 2 | ARCH-013 | PASSED | Add finite nonnegative protein, carbohydrate, and fat fields with at least one positive value and one nullable positive finite Serving base quantity to each Food Object through the next versioned SQL migration, following [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), [REQ-008](../requirements/requirements.md#req-008--one-optional-serving), [REQ-010](../requirements/requirements.md#req-010--valid-macro-profile), and the glossary definitions of [Macro Profile and Serving](../../CONTEXT.md#nutrition). | 1 | Run `go test ./...` from `backend/`. Pass: PostgreSQL integration tests `TestMacroProfileConstraints` and `TestServingConstraints` accept every valid zero-or-one Serving and Macro Profile boundary, reject null macro values, all-zero profiles, negative values, NaN, positive or negative infinity, and nonpositive or nonfinite Servings, and retain zero seed rows (`P01-G3`, `P01-G4`). |
| 3 | ARCH-013 | PASSED | Add the positive-ID Food Family table and one nullable Food Family foreign key on each Food Object, with no hierarchy or second membership representation, through a versioned SQL migration following [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), [REQ-009](../requirements/requirements.md#req-009--one-optional-food-family), and the [Food Family glossary contract](../../CONTEXT.md#food-objects). | 2 | Run `go test ./...` from `backend/`. Pass: PostgreSQL integration test `TestFoodFamilyConstraints` accepts zero or one valid membership, rejects nonpositive family IDs and missing-family references, confirms one nullable foreign key is the only membership path and the Food Family table has no hierarchy column, and retains zero seed rows (`P01-G3`, `P01-G4`). |
| 4 | ARCH-007, ARCH-013, ARCH-016 | PASSED | Use separate PostgreSQL login credentials for setup and runtime access. Local deployment setup and integration fixtures create both users before `dbsetup` runs; `dbsetup` reads the schema-owner connection from `OBIAD_SCHEMA_OWNER_DATABASE_URL`, and the later Fiber process reads the SELECT-only connection from `OBIAD_RUNTIME_DATABASE_URL`. Grant the runtime user `SELECT` on the Food Catalog tables, remove `PUBLIC` object-creation and temporary-table privileges, and retain an empty catalog, following [ARCH-007](../architecture/architecture.md#arch-007--database-setup-module), [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), and [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment). | 3 | Run `go run ./cmd/dbsetup` against an empty disposable owner database and run `go test ./...` from `backend/`. Pass: real PostgreSQL integration test `TestDatabaseCredentialSeparation` shows that the owner applies the complete schema, the runtime credential can SELECT, and it cannot INSERT, UPDATE, DELETE, create tables, or create temporary tables; every catalog table remains empty and the complete backend suite passes (`P01-G1`, `P01-G2`). |

## Phase 2 — Deterministic Food Catalog seed

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 5 | ARCH-007, ARCH-013 | PASSED | Add the optional opaque frontend `image_key` to each Food Object through the next versioned SQL migration, while keeping derived calories, Nutritional Similarities, Matched Quantities, page data, and rounded display values out of production tables, following [ARCH-007](../architecture/architecture.md#arch-007--database-setup-module), [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), and [REQ-011](../requirements/requirements.md#req-011--image-placeholder). | 4 | Run `go test ./...` from `backend/`. Pass: PostgreSQL integration test `TestFoodObjectImageKey` proves that `image_key` is nullable, preserves an opaque key exactly, and leaves the production schema limited to the ARCH-013 source fields; every existing schema-constraint and credential-separation integration check still passes. |
| 6 | ARCH-007, ARCH-013, ARCH-016 | PASSED | Create an embedded, versioned, transaction-safe seed SQL migration for the owner-approved catalog in ISSUE-002: exactly 38 generic Food Objects with fixed IDs `1` through `38`, plausible test-designed source nutrition, and the approved localized names, Physical States, Servings, Food Family membership, and image keys, following the [catalog-seed plan](plan.md#phase-2--deterministic-food-catalog-seed), [ARCH-007](../architecture/architecture.md#arch-007--database-setup-module), [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment), [REQ-004](../requirements/requirements.md#req-004--generic-food-objects), [REQ-070](../requirements/requirements.md#req-070--deterministic-database-setup), and the [Food Object glossary contract](../../CONTEXT.md#food-objects). Adapt the existing PostgreSQL integration checks to assert against the seeded baseline rather than an empty catalog. | 5 | Run `go run ./cmd/dbsetup` twice against one empty-at-start disposable PostgreSQL database, then run `go test ./...` from `backend/`. Pass: integration tests `TestDBSetupAppliesVersionedMigrations`, `TestDeterministicCatalogSeed`, and `TestDatabaseCredentialSeparation` show that the first command atomically inserts the exact 38-row ISSUE-002 catalog, the second applies zero migrations, complete ordered Food Family and Food Object snapshots after both runs are identical, every approved generic name and fixed ID is present, and the SELECT-only credential reads but cannot change the seeded catalog (`P02-G1`, `P02-G2`, `P02-G3`, `P02-G4`). |
| 7 | ARCH-007, ARCH-013, ARCH-018 | PASSED | Add PostgreSQL integration coverage for the observable catalog produced by a fresh setup. Designate Pizza Margherita, Chicken breast, and Milk by their approved fixed IDs and localized names; verify their stored Physical States, Servings, Food Family membership, and image keys; verify the catalog contains solid and liquid rows, rows with and without Servings, and rows with and without images; and count rows remaining after applying only input and Food Family exclusion. Defer Nutritional Similarities, Matched Quantities, ranking, and paging assertions until the corresponding production modules exist, following the [catalog-seed plan](plan.md#phase-2--deterministic-food-catalog-seed), [ARCH-007](../architecture/architecture.md#arch-007--database-setup-module), [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), [ARCH-018](../architecture/architecture.md#arch-018--nutrition-and-paging-mechanism), [REQ-033](../requirements/requirements.md#req-033--food-family-exclusion), and [REQ-071](../requirements/requirements.md#req-071--catalog-coverage). This task does not verify [REQ-072](../requirements/requirements.md#req-072--test-designed-nutrition), which remains deferred until the Phase 4 calculation code exists. | 6 | Run `go test ./...` from `backend/`. Pass: PostgreSQL integration test `TestAcceptanceCatalogCoverage` loads a fresh setup; observes exactly 38 Food Objects; resolves each designated ID and both localized names exactly once; verifies the designated source fields; counts `36`, `37`, and `37` catalog rows respectively after applying only input and Food Family exclusion; confirms Pizza Capricciosa shares Pizza Margherita's Food Family but remains available to inputs outside that family; and confirms the seeded source data covers both Physical States, optional Servings, and optional images without asserting unimplemented similarity, quantity, ranking, or paging behavior. |

## Phase 3 — Food Object suggestion API

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 8 | ARCH-008, ARCH-022 | PASSED | Check in the authoritative OpenAPI document for `GET /api/v1/food-suggestions`, including the resolved five-item success envelope, localized names, default Food Quantity, and suggestion-relevant stable error schemas. Pin reproducible generation commands for Go transport models and the TypeScript client and types, and create only the minimal `frontend/` Bun package needed to compile the generated client, with no Svelte, Vite, `/api` proxy, or interaction code, following the [suggestion-API plan](plan.md#phase-3--food-object-suggestion-api), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), and the [tech stack](../architecture/tech-stack.md#tech-stack). Resolve ISSUE-004 before preparing the transport contract. | 7 | Run `go generate ./...` twice and `go test ./...` from `backend/`, then run `bun run generate:api` twice and `bun run typecheck` from `frontend/`. Pass: the ISSUE-004-resolved Go artifact and generated TypeScript client and types compile, each second generation is byte-stable, the generated values preserve the authoritative operation and schemas, and the frontend package contains no Svelte or Vite dependency (`P03-G1`). |
| 9 | ARCH-007, ARCH-016, ARCH-022 | PASSED | Extract the existing disposable-PostgreSQL setup into reusable backend integration-test support that creates and cleans up isolated databases plus schema-owner, SELECT-only runtime, and unprivileged roles, then migrate the existing database-setup tests to that support without changing their behavior. Expose only the owner/runtime controls needed by later fresh-snapshot, ranking-fixture, database-outage, and deadline scenarios, following [ARCH-007](../architecture/architecture.md#arch-007--database-setup-module), [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), and ISSUE-001. | 7 | Run `go test ./...` from `backend/`. Pass: `TestDBSetupAppliesVersionedMigrations`, `TestDatabaseCredentialSeparation`, and `TestAcceptanceCatalogCoverage` use the shared support and still pass against real PostgreSQL; each test receives unique owner and runtime URLs, retains the privilege boundary, and removes its database and roles on success or failure. |
| 10 | ARCH-006, ARCH-013, ARCH-016, ARCH-022 | PASSED | Add the private concrete PostgreSQL Catalog Loader that executes one fresh embedded SELECT per operation from SQL colocated under `backend/internal/repository/sql/`, binds all dynamic SQL values through pgx parameters, maps rows to private Food Object values, validates catalog invariants, and distinguishes storage from invariant failures. Add no exported repository interface, fake Adapter, runtime cache, SQL ranking, automatic retry, or derived-value persistence, following the [suggestion-API plan](plan.md#phase-3--food-object-suggestion-api), [ARCH-006](../architecture/architecture.md#arch-006--postgresql-catalog-loader), [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), and [ADR 0001](../architecture/0001-localized-names-jsonb.md#store-localized-names-in-food-object-jsonb). | 9 | Run `go test ./...` from `backend/`. Pass: PostgreSQL integration tests `TestCatalogLoaderIntegration` and `TestCatalogLoaderReadsFreshSnapshot` load all 38 seeded Food Objects with exact IDs, names, Physical States, and optional Servings through the runtime credential; observe an owner-made valid fixture change on the next load; trace exactly one SELECT and no mutation per load; and classify real storage and catalog-invariant failures without a cache or retry. |
| 11 | ARCH-004, ARCH-006, ARCH-017, ARCH-022 | PASSED | Implement the concrete Suggest Food Objects `Run` operation over the Catalog Loader. Validate raw UTF-8; normalize the Search Query and compared active-language names to NFC; trim and collapse Unicode whitespace to ASCII space; apply Unicode lowercase mapping; reject normalized empty or over-128-code-point queries; compute raw code-point Levenshtein distance with memory bounded by the shorter input; and return exactly five distinct suggestions ordered by exact-match, full-name-prefix, substring, and fallback tier, then by distance, the ISSUE-004-resolved pinned English or Polish collation, and stable Food Object ID within each tier. Return both names and a default of `1 serving`, `100 g`, or `100 ml` without generated transport values entering the Module, following [ARCH-004](../architecture/architecture.md#arch-004--suggest-food-objects-module), [ARCH-006](../architecture/architecture.md#arch-006--postgresql-catalog-loader), [ARCH-017](../architecture/architecture.md#arch-017--suggestion-ranking-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-002](../requirements/requirements.md#req-002--seeded-catalog-source), [REQ-007](../requirements/requirements.md#req-007--nutrition-basis), [REQ-012](../requirements/requirements.md#req-012--five-suggestions), [REQ-013](../requirements/requirements.md#req-013--interface-language-search), [REQ-014](../requirements/requirements.md#req-014--query-normalization), [REQ-015](../requirements/requirements.md#req-015--polish-characters), [REQ-017](../requirements/requirements.md#req-017--suggestion-tie-order), [REQ-076](../requirements/requirements.md#req-076--autocomplete-match-order), [REQ-023](../requirements/requirements.md#req-023--serving-default), [REQ-024](../requirements/requirements.md#req-024--nutrition-basis-default), and the [domain glossary](../../CONTEXT.md#search). | 10 | Run `go test ./...` from `backend/`. Pass: real-PostgreSQL integration tests `TestSuggestFoodObjectsIntegration` and `TestSuggestionRankingMemoryBound` exercise the concrete operation and fresh Catalog Loader; prove five distinct results for normal and no-close-match queries in both languages; prove all three default quantities, normalization, Polish-diacritic distance, exact/prefix/substring/fallback tier order, within-tier raw-distance order, active-language and ID tie order, and stable validation failures; and use a 128-code-point query plus a long database name to fail full-matrix memory growth while accepting bounded-row growth. |
| 12 | ARCH-009, ARCH-016, ARCH-022 | PASSED | Create `backend/cmd/server` and the Fiber v3 application composition. Read `OBIAD_RUNTIME_DATABASE_URL`, configure pgx with zero minimum and four maximum connections, bind only to the ISSUE-004-resolved loopback address, and add unversioned `GET /health` with a bounded PostgreSQL ping and only the exact ready and unavailable responses. Add no CORS, TLS, authentication, cookies, rate limiter, or third-party runtime service, following the [suggestion-API plan](plan.md#phase-3--food-object-suggestion-api), [ARCH-009](../architecture/architecture.md#arch-009--backend-readiness-interface), [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), and the [backend tech stack](../architecture/tech-stack.md#backend). | 9 | Run `go test ./...` from `backend/`. Pass: integration tests `TestServerRuntimeConstraints` and `TestHealthHTTPIntegration` start an actual loopback Fiber listener with disposable PostgreSQL; observe `200 {"status":"ready"}` while the runtime credential can ping, `503 {"status":"unavailable"}` after that database becomes unusable, no dependency details in either body, a zero-minimum/four-maximum pool using only the runtime URL, and no non-loopback listener (`P03-G3`). |
| 13 | ARCH-004, ARCH-008, ARCH-017, ARCH-022 | PASSED | Add the Fiber Adapter for `GET /api/v1/food-suggestions`. Map generated query and response values only at the HTTP boundary, call the concrete `Run` operation, and serialize exactly five items with stable Food Object IDs, both localized names, and backend-derived default Food Quantities using the ISSUE-004-resolved OpenAPI shape, following [ARCH-004](../architecture/architecture.md#arch-004--suggest-food-objects-module), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-017](../architecture/architecture.md#arch-017--suggestion-ranking-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-002](../requirements/requirements.md#req-002--seeded-catalog-source), [REQ-007](../requirements/requirements.md#req-007--nutrition-basis), [REQ-012](../requirements/requirements.md#req-012--five-suggestions), [REQ-013](../requirements/requirements.md#req-013--interface-language-search), [REQ-014](../requirements/requirements.md#req-014--query-normalization), [REQ-015](../requirements/requirements.md#req-015--polish-characters), [REQ-016](../requirements/requirements.md#req-016--levenshtein-order), [REQ-017](../requirements/requirements.md#req-017--suggestion-tie-order), [REQ-023](../requirements/requirements.md#req-023--serving-default), and [REQ-024](../requirements/requirements.md#req-024--nutrition-basis-default). | 8, 11, 12 | Run `go test ./...` from `backend/`. Pass: HTTP integration tests `TestFoodSuggestionsHTTPIntegration` and `TestFoodSuggestionRankingHTTPIntegration` use an actual loopback Fiber listener and real PostgreSQL; return five distinct seeded suggestions for a normal query and `zzzzzz` in English and Polish; return `1 serving`, solid `100 g`, and liquid `100 ml` defaults with both names; give case and mixed-Unicode-space variants the same order; treat `z` and `ż` as one edit apart; and prove expected raw-distance, active-language-collation, and stable-ID tie orders with isolated database fixtures (`P03-G4`, `P03-G5`, `P03-G6`, `P03-G7`, `P03-G8`). |
| 14 | ARCH-008, ARCH-016, ARCH-019, ARCH-022 | PASSED | Complete suggestion request control and failure handling. Derive a 450 ms context in Fiber and pass it through the operation to pgx; perform no retry; map normalized-empty, overlong, unsupported-language, malformed, invalid-UTF-8, catalog-unavailable, timeout, invariant, and unexpected failures to the ISSUE-004-resolved stable HTTP status, `code`, and optional `field`; and emit structured logs with request ID, method, route template, status, duration, stable error code, and internal cause while excluding Search Query text, SQL parameters, credentials, stack details, and internal causes from responses, following the [suggestion-API plan](plan.md#phase-3--food-object-suggestion-api), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment), [ARCH-019](../architecture/architecture.md#arch-019--request-control-and-failure-mechanism), and [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism). | 13 | Run `go test ./...` from `backend/`. Pass: integration tests `TestFoodSuggestionValidationHTTPIntegration`, `TestFoodSuggestionFailuresHTTPIntegration`, and `TestSuggestionRequestLogIntegration` send real HTTP requests through Fiber and real pgx; prove empty and Unicode-whitespace-only rejection, the post-normalization 128-code-point boundary, canonical equivalence for decomposed Unicode, and the resolved invalid-input mappings; use isolated real database outage, pool-blocking, and invariant scenarios to prove every suggestion-relevant stable error without response leakage or retry; prove the 450 ms deadline reaches pgx and returns `SEARCH_TIMEOUT`; and prove required log fields and sensitive-value exclusions while the complete backend suite passes (`P03-G2`, `P03-G9`, `P03-G10`). |

## Phase 4 — First Substitute page API

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 15 | ARCH-005, ARCH-008, ARCH-018, ARCH-022 | PASSED | Extend the checked-in authoritative OpenAPI document with the ISSUE-005-resolved strict `application/json` contract for `POST /api/v1/substitutes/search`. Define a closed request object with positive `int32` `foodObjectId`, a closed `quantity` object containing `double` `value` and unit `g`, `ml`, or `serving`, and nonnegative `int32` `pageIndex`. Define a closed page-0 response with nonnegative `int32` counts, Boolean `hasMore`, zero to three unique items, omitted-not-null optional `imageKey`, whole nonnegative `int64` `matchedQuantity.value` with unit `g` or `ml`, nonnegative `double` protein, carbohydrate, and fat, and `int32` `similarityPercent` from 0 through 100. Define the fixed error-field enum `foodObjectId`, `quantity`, `quantity.value`, `quantity.unit`, and `pageIndex`; continue generating Go transport models and the TypeScript client and types without letting generated values enter the operation Module, following the [page-0 API plan](plan.md#phase-4--first-substitute-page-api), [ARCH-005](../architecture/architecture.md#arch-005--find-substitute-page-module), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-018](../architecture/architecture.md#arch-018--nutrition-and-paging-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-025](../requirements/requirements.md#req-025--quantity-syntax), and the [backend tech stack](../architecture/tech-stack.md#backend). | 14 | Run `go generate ./...` twice and `go test ./...` from `backend/`, then run `bun run generate:api` twice and `bun run typecheck` from `frontend/`. Pass: both second generations are byte-stable; the generated Go transport boundary compiles; the generated TypeScript client and types expose every exact ISSUE-005 request, success, and error type and constraint; and no generated source is committed (`P04-G1`). |
| 16 | ARCH-005, ARCH-006, ARCH-013, ARCH-018, ARCH-022 | PASSED | Implement the concrete Find Substitute Page `Run` operation over the existing private Catalog Loader for a valid page-0 request. Convert the Substitution Input Food Quantity to its base unit; derive calories as `4p + 4c + 9f`; calculate custom `float64` cosine similarity and equal-calorie Matched Quantity; exclude the input and its Food Family; order by decreasing unrounded similarity, pinned English-name collation, and stable Food Object ID; and return total eligible count, `hasMore`, and the first zero to three unique display-ready items. Keep one fresh SELECT, SELECT-only access, generated transport isolation, and no persisted derived values. Use ISSUE-005's absolute `1e-12` tolerance only for test comparison, never as a production tie or ranking threshold, following the [page-0 API plan](plan.md#phase-4--first-substitute-page-api), [ARCH-005](../architecture/architecture.md#arch-005--find-substitute-page-module), [ARCH-006](../architecture/architecture.md#arch-006--postgresql-catalog-loader), [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), [ARCH-018](../architecture/architecture.md#arch-018--nutrition-and-paging-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-029](../requirements/requirements.md#req-029--derived-calories), [REQ-030](../requirements/requirements.md#req-030--nutritional-similarity), [REQ-031](../requirements/requirements.md#req-031--matched-quantity), [REQ-032](../requirements/requirements.md#req-032--input-exclusion), [REQ-033](../requirements/requirements.md#req-033--food-family-exclusion), [REQ-034](../requirements/requirements.md#req-034--similarity-order), [REQ-035](../requirements/requirements.md#req-035--result-tie-order), [REQ-072](../requirements/requirements.md#req-072--test-designed-nutrition), and the [nutrition glossary](../../CONTEXT.md#nutrition). | 14 | Run `go test ./...` from `backend/`. Pass: real-PostgreSQL integration test `TestFindSubstitutePageIntegration` drives the concrete `Run` operation through the SELECT-only Catalog Loader and verifies designated seeded calories, Matched Quantities, eligible counts, page-0 IDs, `hasMore`, input and Food Family exclusions, decreasing full-precision order, English-name and stable-ID ties, three unique results, one fresh SELECT, and no mutation or derived-value persistence. In the same integration test, Macro Profiles loaded by the real private Catalog Loader are passed directly to the private production calorie, cosine, and Matched Quantity helpers and compared with independently recorded full-precision expectations using `abs(got - want) <= 1e-12`; no exported seam, fake, or test hook is added (`P04-G3`). |
| 17 | ARCH-005, ARCH-013, ARCH-018, ARCH-022 | PASSED | Complete display projection inside the Find Substitute Page Module without changing candidate eligibility or order: use the unrounded Matched Quantity to scale protein, carbohydrate, and fat; round Matched Quantity to a whole candidate base unit, each scaled macronutrient to `0.1 g`, and `100 × similarity` to a whole percentage; make exact nonnegative halves round up; and permit a positive value to display as zero. Keep every ranking and calculation in `float64` until this final projection and keep artificial boundary data outside the production seed, following [ARCH-005](../architecture/architecture.md#arch-005--find-substitute-page-module), [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), [ARCH-018](../architecture/architecture.md#arch-018--nutrition-and-paging-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-031](../requirements/requirements.md#req-031--matched-quantity), [REQ-039](../requirements/requirements.md#req-039--display-precision), [REQ-040](../requirements/requirements.md#req-040--calculation-precision), and ISSUE-002. | 16 | Run `go test ./...` from `backend/`. Pass: real-PostgreSQL integration test `TestSubstituteProjectionIntegration` uses isolated Macro Profile fixtures and the concrete `Run` operation to prove full-precision ranking and scaling, solid `g` and liquid `ml` Matched Quantity units, whole-unit, one-decimal, and whole-percentage projection, exact-half-up behavior at every target precision, a positive value that displays as zero, and a fixture that fails if any intermediate value is rounded early (`P04-G4`). |
| 18 | ARCH-005, ARCH-006, ARCH-008, ARCH-018, ARCH-022 | PASSED | Complete Find Substitute Page input validation and stable Module failures. Accept only positive integer direct grams or millilitres and positive fractional Serving counts; require the direct unit to match Physical State, require a stored Serving for `serving`, and require the converted base quantity to be at most `100,000 g` or `100,000 ml`. Reject a nonpositive Food Object ID as `INVALID_REQUEST`, a missing positive catalog ID as `FOOD_OBJECT_NOT_FOUND`, a negative page as `INVALID_PAGE_INDEX`, and every `pageIndex > 0` as `PAGE_OUT_OF_RANGE` until Phase 11. Classify invalid values or units, unit mismatch, unavailable Serving, and over-limit converted values with the ISSUE-005-resolved stable codes, without a retry, fake Adapter, or second catalog read, following the [page-0 API plan](plan.md#phase-4--first-substitute-page-api), [ARCH-005](../architecture/architecture.md#arch-005--find-substitute-page-module), [ARCH-006](../architecture/architecture.md#arch-006--postgresql-catalog-loader), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-018](../architecture/architecture.md#arch-018--nutrition-and-paging-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), and [REQ-025](../requirements/requirements.md#req-025--quantity-syntax). | 16 | Run `go test ./...` from `backend/`. Pass: real-PostgreSQL integration test `TestFindSubstitutePageValidationIntegration` proves accepted integer `g` and `ml` values, dot-decimal Serving values, and both converted quantity boundaries; proves exact stable failures for nonpositive or fractional base values, unsupported units, invalid Serving values, unit mismatch, missing Serving, over-limit converted values, nonpositive or absent Food Object IDs, negative pages, and every nonzero page; and traces no retry and at most one fresh SELECT per request (`P04-G4`). |
| 19 | ARCH-005, ARCH-008, ARCH-016, ARCH-019, ARCH-022 | PASSED | Add the Fiber Adapter for `POST /api/v1/substitutes/search`. Accept only `application/json`; strictly decode one closed generated request object; reject empty or malformed JSON, trailing JSON, unknown keys, and duplicate keys at every nesting level; map valid generated values to the concrete Find Substitute Page operation; and serialize the exact generated page-0 envelope with omitted-not-null optional image keys. Preserve the existing loopback-only composition and keep generated values out of the Module, following ISSUE-005, [ARCH-005](../architecture/architecture.md#arch-005--find-substitute-page-module), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment), [ARCH-019](../architecture/architecture.md#arch-019--request-control-and-failure-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), and the [page-0 API plan](plan.md#phase-4--first-substitute-page-api). | 15, 17, 18 | Run `go test ./...` from `backend/`. Pass: HTTP integration tests `TestSubstituteSearchHTTPIntegration` and `TestSubstituteSearchContractHTTPIntegration` send real POST requests through an actual loopback Fiber listener and real PostgreSQL; prove the exact strict success field sets with no unknown fields, designated page-0 order and values, three unique items, `totalEligibleCount`, `hasMore`, both localized names, omitted and present image keys, `serving`, `g`, and `ml` inputs, and solid `g` plus liquid `ml` whole Matched Quantity outputs (`P04-G4`). |
| 20 | ARCH-005, ARCH-006, ARCH-008, ARCH-016, ARCH-019, ARCH-022 | PASSED | Complete Substitute request control and failure handling. Enforce the 4 KiB request-body limit; derive one 450 ms Fiber context and pass it through pool acquisition, the concrete operation, and pgx; perform no retry; and reuse structured request logging while excluding Food Quantities, request bodies, SQL parameters, credentials, stack details, and internal causes from responses. Apply ISSUE-005's exact error contract: structural failures use `INVALID_REQUEST`; semantic quantity, unit, Serving, range, and page failures use their specific stable codes; a missing positive Food Object uses `FOOD_OBJECT_NOT_FOUND`; a body over 4 KiB uses `REQUEST_BODY_TOO_LARGE`; and storage, deadline, invariant, or unexpected failures use `CATALOG_UNAVAILABLE`, `SEARCH_TIMEOUT`, or `INTERNAL_ERROR`. Return a known field path only for an unambiguous known client field and omit `field` for malformed or unknown payload structure, body-limit failures, and server failures, following [ARCH-005](../architecture/architecture.md#arch-005--find-substitute-page-module), [ARCH-006](../architecture/architecture.md#arch-006--postgresql-catalog-loader), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment), [ARCH-019](../architecture/architecture.md#arch-019--request-control-and-failure-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), and ISSUE-003. | 19 | Run `go test ./...` from `backend/`. Pass: HTTP integration tests `TestSubstituteSearchValidationHTTPIntegration`, `TestSubstituteSearchFailuresHTTPIntegration`, and `TestSubstituteRequestLogIntegration` use actual loopback Fiber and real PostgreSQL to prove the accepted 4 KiB and rejected 4 KiB-plus-one boundaries; strict content-type, empty, malformed, trailing, unknown, duplicate, missing, null, wrong-type, and unsupported-unit handling; every applicable `400`, `404`, `413`, `422`, `503`, `504`, and `500` stable error with the exact ISSUE-005 field or omission and no response leakage; `PAGE_OUT_OF_RANGE` for every nonzero page; deadline propagation into blocked pgx work; no retry; and required sanitized log fields. The explicit ISSUE-003 and ISSUE-005 zero-result deviation is preserved rather than introducing a fake or unsupported catalog fixture, and the complete backend suite passes (`P04-G2`, `P04-G4`). |

## Phase 5 — Empty browser shell

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 21 | ARCH-001, ARCH-008, ARCH-016 | PASSED | Turn the existing compile-only `frontend/` package into a runnable client-only Svelte 5 and Vite application foundation while preserving the generated TypeScript HTTP client and types. Add locked direct dependencies and configuration for Svelte 5, Vite, Tailwind CSS, TanStack Query, and Playwright; add Svelte-aware `dev`, optimized `build` and `preview`, and `typecheck` scripts; and mount a minimal root application with a TanStack Query client available but no startup request, Interface Language control, suggestions, selected input, result cards, or result state, following the [empty-shell plan](plan.md#phase-5--empty-browser-shell), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment), and the [frontend tech stack](../architecture/tech-stack.md#frontend). | 14 | Run `bun install --frozen-lockfile && bun run generate:api && bun run typecheck && bun run build` from `frontend/`. Pass: the frozen dependency graph installs, the existing generated client and hand-written Svelte boundary compile under the Svelte-aware checker, and Vite emits an optimized client-only production build with the TanStack Query provider and no server-rendering or backend code. |
| 22 | ARCH-008, ARCH-016, ARCH-022 | PASSED | Configure Vite development and preview to proxy same-origin `/api` requests to the fixed loopback Fiber listener without adding CORS, then put a self-cleaning real-stack Playwright launcher behind `bun run test:e2e`. Under the lifecycle contract in ISSUE-006, the launcher must build before preview, create a disposable loopback-only PostgreSQL 17 container, reuse the existing [local database setup](../../scripts/setup_local_database.sh) with ephemeral credentials, start the real Fiber process and optimized Vite preview, run pinned Chromium, and clean up every owned process, container, temporary credential file, and generated client output after success, failure, or interruption. Add a stack smoke scenario that uses the generated TypeScript client through the Vite origin, following the [empty-shell plan](plan.md#phase-5--empty-browser-shell), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-016](../architecture/architecture.md#arch-016--local-three-process-deployment), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), and the [operational tech stack](../architecture/tech-stack.md#operational). | 21 | Run `bun run test:e2e` from `frontend/`. Pass: the command needs no prestarted service; creates and seeds disposable PostgreSQL; starts real Fiber and an optimized Vite preview; has the generated client receive the five-item seeded response through the preview origin's `/api` proxy; makes no direct browser-to-Fiber or third-party runtime request; exits nonzero on an unhealthy process; and leaves no owned process, container, credential file, or generated output behind (`P05-G1`, `P05-G2`). |
| 23 | ARCH-001, ARCH-015, ARCH-020, ARCH-022 | PASSED | Apply the ISSUE-006 presentation contract. Mirror the established mealswapp typography pattern with Tailwind UI and data font tokens, system-local `@font-face` ranges for Inter and Roboto Mono, and `system-ui` and `ui-monospace` fallbacks, with no bundled font or runtime font request. Add the owner-supplied `frontend/src/lib/assets/food-placeholder.png` as a `512×512` lossless sRGB image with optional alpha, no localized text or unnecessary metadata, and recorded ownership plus any required attribution. Extend Tailwind with every exact palette token and make the placeholder available to later cards without rendering a card or adding an external runtime asset source, following the [empty-shell plan](plan.md#phase-5--empty-browser-shell), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-015](../architecture/architecture.md#arch-015--static-presentation-assets), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), the [style specification](../requirements/style.md), and the [data-layer tech stack](../architecture/tech-stack.md#data-layer). | 22 | Run `bun run test:e2e` from `frontend/`. Pass: Playwright integration scenario `presentation-assets.spec.ts` loads the optimized real stack; verifies the exact computed palette plus `Inter, system-ui, sans-serif` and `Roboto Mono, ui-monospace, monospace` fallback chains; observes no runtime font or third-party asset request; fetches the placeholder from the Vite origin; and proves it decodes as a `512×512` PNG. |
| 24 | ARCH-001, ARCH-020, ARCH-022 | PASSED | Render only the empty-state Search control in one semantic primary content column using a component under `frontend/src/lib/components/`. Use an `<input type="search">` with a visually hidden `Search` label, `Search foods` placeholder, no icon, and no autofocus. Make the field `56px` high and `min(100%, 640px)` wide inside the centered maximum-`1280px` column, with horizontal page gutters of `16px` below `640px`, `24px` from `640px` through `1023px`, and `32px` from `1024px`; center its box horizontally and place its vertical center at `45%` of `100dvh`. Use the style-specified Surface, Secondary, Text-Primary, and Primary focus treatment. Add no Interface Language controls, suggestion list, selected input, result cards, result-state layout, or application API request. Add the real-stack responsive and visual Playwright coverage for [REQ-060](../requirements/requirements.md#req-060--empty-state-layout), following ISSUE-006, the [empty-shell plan](plan.md#phase-5--empty-browser-shell), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), and the [style specification](../requirements/style.md). | 23 | Run `bun run test:e2e` from `frontend/`. Pass: Playwright integration scenario `empty-shell.spec.ts` observes the exact label, placeholder, input semantics, and initial focus state; observes exactly one primary column and one Search control, no excluded Phase 6 or Phase 7 region, and no application API request; proves the column cap, responsive gutters, input and visible focus styles, and no horizontal overflow; measures the Search box within `1` CSS px of the specified horizontal and `45%` vertical centers at `320×568`, `768×1024`, and `1280×720`; and records exactly three full-page PNG review attachments without using pixel comparison as a gate (`P05-G3`, `P05-G4`, `REQ-060`). |

## Phase 6 — Interface Language preference

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 25 | ARCH-001, ARCH-003, ARCH-012, ARCH-014, ARCH-022 | PASSED | Implement the initial Interface Language slice through `frontend/src/lib/i18n.ts` and `frontend/src/lib/interfaceLanguage.ts`: define a closed `en` and `pl` type; expose every currently rendered interface and accessibility message through shape-checked static dictionary functions; and move the existing Search label and placeholder out of `Search.svelte`. Fix the English copy as `Search`, `Search foods`, and `Interface language`, and the Polish copy as `Szukaj`, `Szukaj potraw`, and `Język interfejsu`. Initialize one persisted Svelte store before the first application render. An exact valid value from the `obiad.interfaceLanguage` `localStorage` key wins; otherwise inspect `navigator.languages` in order and case-insensitively choose the first supported primary language, then default to English. Use no cookie. Treat a failed storage read as missing, ignore any other stored value without rewriting it, and do not persist a browser-derived initial choice. The store setter updates memory before attempting persistence and retains the in-memory selection without throwing when a write fails. Make the existing Search control consume the active dictionary without adding an Interface Language control, an HTTP request, current-result translation, suggestions, or Search-field language-change orchestration. Pin `happy-dom` and `@testing-library/svelte` for component integration of the store-to-application seam; keep full-deployment acceptance in Playwright, following ISSUE-007, the [Interface Language preference plan](plan.md#phase-6--interface-language-preference), [ARCH-003](../architecture/architecture.md#arch-003--translation-module), the initial-selection behavior of [ARCH-012](../architecture/architecture.md#arch-012--interface-language-change-collaboration), [ARCH-014](../architecture/architecture.md#arch-014--interface-language-preference), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-056](../requirements/requirements.md#req-056--initial-language), the [frontend tech stack](../architecture/tech-stack.md#frontend), and ISSUE-006. | 24 | Run `bun run typecheck`, `bun test`, and `bun run test:e2e` from `frontend/`. Pass: happy-dom component integration scenario `App.language-initialization.test.ts` proves the root application and existing Search component share the typed active dictionary for the exact English and Polish copy and verifies graceful read and write failure behavior without a cookie or network call; real-stack Playwright scenario `interface-language-initialization.spec.ts` observes Polish on `pl-PL`, English on `en-US` and `de-DE`, the first supported primary language in an ordered multi-language list, valid saved `en` and `pl` values overriding the browser, and missing or invalid saved values invoking browser resolution. It also proves the first rendered Search label and placeholder use the resolved dictionary, browser-derived initialization performs no storage write, and startup performs no application API request (`P06-G1`, `P06-G2`, `P06-G3`, `REQ-056`). |
| 26 | ARCH-001, ARCH-003, ARCH-012, ARCH-014, ARCH-020, ARCH-022 | PASSED | Add `frontend/src/lib/components/InterfaceLanguage.svelte` as a borderless native dropdown in the primary column's top-right corner without changing the Search field's established `45dvh` center. Show only the active `PL` or `EN` language code and a small downward chevron, with fixed PL-then-EN options and the localized accessible name `Interface language` or `Język interfejsu`. Use the existing responsive gutter as both top and right inset: `16px` below `640px`, `24px` from `640px` through `1023px`, and `32px` from `1024px`. Keep a transparent background, no resting or hover border, a minimum `44px` target, and a two-pixel Primary focus-visible outline with two-pixel offset. Bind native pointer and keyboard selection directly to the persisted Interface Language store so each selection immediately applies the corresponding typed dictionary and attempts to save the exact `pl` or `en` value to `obiad.interfaceLanguage`; if saving is blocked, retain the selected language in memory without an error or cookie. A reload resolves from a successfully saved value before browser languages. Keep empty-state Search geometry and API inactivity, and add no current-result translation, suggestions, result state, or explicit Search focus, text-retention, or suggestion-closing collaboration, following ISSUE-007, the [Interface Language preference plan](plan.md#phase-6--interface-language-preference), [ARCH-003](../architecture/architecture.md#arch-003--translation-module), the selection boundary of [ARCH-012](../architecture/architecture.md#arch-012--interface-language-change-collaboration), [ARCH-014](../architecture/architecture.md#arch-014--interface-language-preference), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-057](../requirements/requirements.md#req-057--language-preference), and the [style specification](../requirements/style.md). | 25 | Run `bun run typecheck`, `bun test`, and `bun run test:e2e` from `frontend/`. Pass: happy-dom component integration scenario `InterfaceLanguage.test.ts` observes one localized native dropdown, fixed PL-then-EN options, the active value, immediate shared-dictionary update, one persistence attempt per selection, and session-only success when persistence throws; real-stack Playwright scenario `interface-language-selection.spec.ts` selects PL and EN, observes exact localized Search and control copy, reads the exact saved `pl` or `en` value, reloads, and observes the saved language before a conflicting browser locale. At `320px`, `768px`, and `1280px`, the dropdown has at least a `44px` target, remains visible, keyboard reachable, visibly focused, non-overflowing, at the responsive top-right inset, transparent and borderless with only the active language and chevron visible, without moving the Search field; the scenario observes no application API request or excluded result/Search-interaction behavior (`P06-G4`, `REQ-057`). |

## Phase 7 — Pointer Substitution Search

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 27 | ARCH-001, ARCH-002, ARCH-008, ARCH-010, ARCH-019, ARCH-020, ARCH-022 | PASSED | Add the live Food Object suggestion slice to the existing Search control. Establish the Phase-7 subset of the discriminated browser-interaction state for Search Query text and focus; call `GET /api/v1/food-suggestions` only through the generated TypeScript client and TanStack Query while the Search field is focused and contains nonempty text; key each request by Search Query and active Interface Language; pass TanStack Query's `AbortSignal`; disable automatic retry and successful-response reuse; and ensure that neither an aborted request nor an out-of-order response can replace the current list. Render exactly the returned five localized options in a panel that matches Search's maximum `640px` width, starts `8px` below it, and contains five `48px` rows. Use Surface with a Secondary border, and render the first option with Primary and Text-On-Bright. Establish baseline combobox/listbox semantics, stable option IDs, and the Search input's `aria-activedescendant`; task 31 completes the same phase's key transitions. Add no normalized-empty validation, suggestion failure UI, or copy of HTTP data into a Svelte store, following the [Pointer Substitution Search plan](plan.md#phase-7--pointer-substitution-search), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-002](../architecture/architecture.md#arch-002--browser-interaction-module), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-010](../architecture/architecture.md#arch-010--food-suggestion-collaboration), [ARCH-019](../architecture/architecture.md#arch-019--request-control-and-failure-mechanism), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-001](../requirements/requirements.md#req-001--anonymous-access), [REQ-002](../requirements/requirements.md#req-002--seeded-catalog-source), [REQ-012](../requirements/requirements.md#req-012--five-suggestions), [REQ-013](../requirements/requirements.md#req-013--interface-language-search), [REQ-018](../requirements/requirements.md#req-018--first-suggestion-highlight), the [frontend tech stack](../architecture/tech-stack.md#frontend), ISSUE-004, and ISSUE-008. | 20, 26 | Run `bun run typecheck`, `bun test`, and `bun run test:e2e` from `frontend/`. Pass: real-stack Playwright scenario `search-suggestions.spec.ts` starts in a fresh unauthenticated browser context, observes no startup application request, focuses and enters a normal Search Query and `zzzzzz`, and sees exactly five distinct seeded suggestions in English and Polish. The panel has the approved width, offset, row heights, resting colors, and active colors; the first option's stable ID is the Search input's `aria-activedescendant`. The scenario controls two responses from the real `/api/v1/food-suggestions` stack to prove that a superseded browser request is aborted and its delayed response cannot change the latest list. Every food-data request stays on the Vite origin under `/api`, and the observed IDs and names match the freshly seeded PostgreSQL catalog (`P07-G2`, `P07-G5`, `P07-G6`, `P07-G7`, `P07-G20`, `REQ-001`, `REQ-002`, `REQ-012`, `REQ-013`, `REQ-018`). |
| 28 | ARCH-001, ARCH-002, ARCH-003, ARCH-008, ARCH-010, ARCH-011, ARCH-019, ARCH-020, ARCH-022 | PASSED | Implement the pointer-selection and new-search transition over the live suggestions. A click or tap on any option selects that exact returned Food Object without moving focus from Search, closes the suggestion list, replaces Search Query text with the selected active-language name, retains the selected localized names and returned default Food Quantity as the read-only Substitution Input, and starts exactly one generated-client `POST /api/v1/substitutes/search` TanStack Query operation with that `foodObjectId`, unchanged `quantity`, and `pageIndex: 0`. Extend the single interaction-state union only with the required `loadingNew`, `results`, and `zeroResults` transitions while TanStack Query continues to own response data and pending state; allow no duplicate intent, queue, automatic retry, second submit action, or response-data store. Render the visible label `Selected food` or `Wybrany produkt` and the value `localized name · quantity unit`; show Serving as `1 serving` or `1 porcja`, keep `g` and `ml` invariant, and do not reactively translate this captured active-content value. Show the new-search spinner `12px` below Search for the complete pending interval, then retain Search focus after success. Add no Food Quantity edit, MORE!, failure state, motion, or active-content language-change behavior, following the [Pointer Substitution Search plan](plan.md#phase-7--pointer-substitution-search), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-002](../architecture/architecture.md#arch-002--browser-interaction-module), [ARCH-003](../architecture/architecture.md#arch-003--translation-module), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-010](../architecture/architecture.md#arch-010--food-suggestion-collaboration), [ARCH-011](../architecture/architecture.md#arch-011--substitution-search-collaboration), [ARCH-019](../architecture/architecture.md#arch-019--request-control-and-failure-mechanism), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-020](../requirements/requirements.md#req-020--pointer-suggestion-selection), [REQ-022](../requirements/requirements.md#req-022--immediate-search), [REQ-023](../requirements/requirements.md#req-023--serving-default), [REQ-024](../requirements/requirements.md#req-024--nutrition-basis-default), [REQ-046](../requirements/requirements.md#req-046--search-spinner), [REQ-064](../requirements/requirements.md#req-064--search-focus), [REQ-077](../requirements/requirements.md#req-077--selected-suggestion-in-search), ISSUE-005, and ISSUE-008. | 27 | Run `bun run typecheck`, `bun test`, and `bun run test:e2e` from `frontend/`. Pass: real-stack Playwright scenario `pointer-substitution-search.spec.ts` selects the third displayed option with a pointer, observes the Search field replace unfinished text with that exact returned English name, and observes one, and only one, generated-client POST whose body carries that option's Food Object ID, unchanged default quantity, and page `0`; the successful page and selected-input region identify the same Food Object without another submit action. Separate seeded Pizza Margherita, Chicken breast, and Milk flows show the exact localized selected label and value, send `1 serving`, `100 g`, and `100 ml`, and retain the captured result language. A controlled response fetched from real Fiber and PostgreSQL remains pending at the browser boundary while the spinner stays `12px` below Search; fulfillment removes the spinner and leaves Search as `document.activeElement` (`P07-G8`, `P07-G9`, `P07-G10`, `P07-G11`, `P07-G17`, `P07-G18`, `P07-G19`, `REQ-020`, `REQ-022`, `REQ-023`, `REQ-024`, `REQ-046`, `REQ-064`). |
| 29 | ARCH-001, ARCH-003, ARCH-015, ARCH-020, ARCH-022 | PASSED | Add a result-card component that consumes one generated `SubstituteItem` without browser-side nutrition calculation or rerounding. Render the Food Object name for the Interface Language captured by the search, then Matched Quantity, protein, carbohydrate, fat, and similarity in that order. Use visible labels `Protein`, `Carbohydrates`, `Fat`, and `Similarity` in English and `Białko`, `Węglowodany`, `Tłuszcz`, and `Podobieństwo` in Polish. Keep Matched Quantity whole with only `g` or `ml`; format every macronutrient with exactly one active-locale decimal place and `g`; and format similarity as a whole percentage. Use an empty supported image-key map: absent keys, the four seeded opaque keys, and every other unmapped key resolve to the existing bundled placeholder. Render every card image with empty alternative text because the adjacent heading carries the Food Object name, and reset any failed image source to the same placeholder. Add no Serving equivalent, card action, paging control, animation, current-result language-change transition, or persisted display value. Put the approved component integration coverage beside the component and real-stack card coverage beside the Phase 7 Playwright flow, following the [Pointer Substitution Search plan](plan.md#phase-7--pointer-substitution-search), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-003](../architecture/architecture.md#arch-003--translation-module), [ARCH-015](../architecture/architecture.md#arch-015--static-presentation-assets), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-011](../requirements/requirements.md#req-011--image-placeholder), [REQ-036](../requirements/requirements.md#req-036--first-result-page), [REQ-037](../requirements/requirements.md#req-037--card-data), [REQ-038](../requirements/requirements.md#req-038--base-unit-matched-quantity), [REQ-039](../requirements/requirements.md#req-039--display-precision), [REQ-040](../requirements/requirements.md#req-040--calculation-precision), the [style specification](../requirements/style.md#5-component-standards-svelte), and ISSUE-008. | 28 | Run `bun run typecheck`, `bun test`, and `bun run test:e2e` from `frontend/`. Pass: happy-dom component integration scenario `ResultCard.test.ts` observes the identical bundled placeholder and empty `alt` for an absent key, each of `pizza-margherita`, `chicken-breast`, `milk`, and `gyoza`, an arbitrary unmapped key, and a dispatched image error, with no third-party request. Real-stack Playwright scenario `result-cards.spec.ts` observes Pizza Margherita ranks `[13, 29, 26]`, Chicken breast ranks `[23, 11, 6]`, and Milk ranks `[33, 3, 21]`; exactly three unique cards expose each API-provided English or Polish name, whole Matched Quantity, protein, carbohydrate, fat, and whole similarity percentage in the approved order and copy. English macronutrients retain a dot and Polish macronutrients a comma with exactly one decimal place, Matched Quantities use `g` or `ml` with no Serving equivalent, and both the image-less Chicken-breast page and the seeded-key Gyoza card show valid placeholder cards (`P07-G12`, `P07-G13`, `P07-G14`, `P07-G15`, `REQ-011`, `REQ-036`, `REQ-037`, `REQ-038`, `REQ-039`, `REQ-040`). |
| 30 | ARCH-001, ARCH-002, ARCH-003, ARCH-011, ARCH-019, ARCH-020, ARCH-022 | PASSED | Compose the completed Phase 7 loading, result, and zero-result surfaces in the root application. Keep one semantic primary content column with stable Search, selected-input, and result regions and retain the existing empty-state Search geometry. From `loadingNew` onward, put the Search field's top edge `96px` from the viewport top, its spinner `12px` below it when pending, and the selected-input and result regions after it at `24px` intervals. Stack the result cards in one column until the later responsive-presentation phase. A successful three-item page renders exactly the three result cards; a successful empty page renders no cards and exactly `No substitutes found` or `Nie znaleziono zamienników`. Keep Search above every result card and preserve the Phase 7 focus transition. Task 31 completes this phase's key transitions; add no editable Food Quantity, MORE!, request failures, result announcements, card motion, final responsive-column work, or active-content language changes, following ISSUE-003, ISSUE-008, the [Pointer Substitution Search plan](plan.md#phase-7--pointer-substitution-search), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-002](../architecture/architecture.md#arch-002--browser-interaction-module), [ARCH-003](../architecture/architecture.md#arch-003--translation-module), [ARCH-011](../architecture/architecture.md#arch-011--substitution-search-collaboration), [ARCH-019](../architecture/architecture.md#arch-019--request-control-and-failure-mechanism), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-003](../requirements/requirements.md#req-003--single-page-interface), [REQ-044](../requirements/requirements.md#req-044--zero-result-state), [REQ-061](../requirements/requirements.md#req-061--result-state-layout), and the [global layout specification](../requirements/style.md#4-global-layout-patterns). | 29 | Run `bun run typecheck`, `bun test`, and `bun run test:e2e` from `frontend/`. Pass: approved happy-dom component integration scenario `App.result-state.test.ts` drives a successful empty response through the production browser state and rendered components in English and Polish and observes the exact localized zero-result message with zero cards and no repository fake. Real-stack Playwright scenario `result-state.spec.ts` runs the complete anonymous pointer flow and observes one primary column containing distinct Search, selected-input, and result regions in that order; after selection, Search is `96px` from the viewport top within `1` CSS px, the regions use the approved vertical gaps, cards remain in one column, Search is geometrically above exactly three cards, and every food-data request uses `/api` against the seeded PostgreSQL catalog. The existing empty-state geometry remains unchanged (`P07-G1`, `P07-G3`, `P07-G4`, `P07-G16`, `REQ-003`, `REQ-044`, `REQ-061`). |
| 31 | ARCH-001, ARCH-002, ARCH-010, ARCH-020, ARCH-022 | PASSED | Complete the Phase 7 suggestion control with keyboard operation over the existing TanStack Query list and the same selection transition used by pointer activation. Arrow Down moves toward the fifth option and clamps there; Arrow Up moves toward the first option and clamps there; every move updates active styling and `aria-activedescendant`. Enter selects the active option and starts the same one default-quantity page-0 search as a pointer. Escape closes the list while retaining Search Query text and Search focus and starts no Substitution Search. Tab closes the list without preventing the browser's native focus movement and starts no Substitution Search. Keep option DOM focus on Search through the active-descendant pattern, retain exactly five options while open, and do not copy HTTP data into local state or change pointer behavior, following the [Pointer Substitution Search plan](plan.md#phase-7--pointer-substitution-search), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-002](../architecture/architecture.md#arch-002--browser-interaction-module), [ARCH-010](../architecture/architecture.md#arch-010--food-suggestion-collaboration), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-018](../requirements/requirements.md#req-018--first-suggestion-highlight), [REQ-019](../requirements/requirements.md#req-019--suggestion-keyboard-control), [REQ-020](../requirements/requirements.md#req-020--pointer-suggestion-selection), [REQ-077](../requirements/requirements.md#req-077--selected-suggestion-in-search), and ISSUE-008. | 30 | Run `bun run typecheck`, `bun test`, and `bun run test:e2e` from `frontend/`. Pass: real-stack Playwright scenario `suggestion-keyboard.spec.ts` observes the first option active, Arrow Up clamped on the first option, Arrow Down moving through all options and clamped on the fifth, and each active option's stable ID reflected by `aria-activedescendant`. Enter on the third option replaces Search text with that option's active-language name and starts exactly one generated-client Substitution Search with that option's returned default quantity and loads its page-0 result. Escape retains Search text and focus with the list closed and zero Substitution Search requests. Tab closes the list, moves focus natively to the next control, and starts zero Substitution Search requests. The existing pointer scenario still passes unchanged (`P07-G21`, `REQ-018`, `REQ-019`, `REQ-020`). |

## Phase 9 — Empty Search Query validation

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 32 | ARCH-001, ARCH-002, ARCH-010, ARCH-017, ARCH-020, ARCH-022 | PASSED | Add the normalized-empty browser no-op to the existing Search control. Decide emptiness exactly by whether the [ARCH-017 normalization contract](../architecture/architecture.md#arch-017--suggestion-ranking-mechanism), including Go-compatible Unicode whitespace such as `U+0085` NEXT LINE, produces an empty Search Query. Keep the exact raw Search Query unchanged in the interaction state and field, and disable the suggestion query for every normalized-empty draft. On Enter with no open suggestion and a normalized-empty value, prevent the browser action, retain the exact raw value, Search focus, and current interaction state, show no validation message or invalid state, and start neither `GET /api/v1/food-suggestions` nor `POST /api/v1/substitutes/search`. Preserve Enter selection for a normalized-nonempty open suggestion and add no Food Quantity validation, translation dictionary entry, backend request, or interaction-state variant, following the [Empty Search Query validation plan](plan.md#phase-9--empty-search-query-validation), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-002](../architecture/architecture.md#arch-002--browser-interaction-module), [ARCH-010](../architecture/architecture.md#arch-010--food-suggestion-collaboration), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-021](../requirements/requirements.md#req-021--empty-search-query), the [Search Query glossary contract](../../CONTEXT.md#search), and ISSUE-009. | 26, 31 | Run `bun run typecheck`, `bun test`, and `bun run test:e2e` from `frontend/` (`P09-G1`). Pass: real-stack Playwright scenario `empty-search-query.spec.ts` presses Enter with the exact raw values empty, ASCII-spaces-only, and mixed `U+0085` NEXT LINE, `U+00A0` NO-BREAK SPACE, `U+2003` EM SPACE, and `U+202F` NARROW NO-BREAK SPACE in one fresh unauthenticated English Chromium context (`P09-G2`). Every case retains its exact field value, Search focus, and `empty` interaction state, renders no validation message or invalid state, and observes zero suggestion GETs and zero Substitution Search POSTs (`P09-G3`, `REQ-021`). The first normalized-nonempty value starts one live suggestion request, while the existing `suggestion-keyboard.spec.ts` Enter-selection path passes unchanged; no editable Food Quantity control or validation appears. |

## Phase 10 — Food Quantity editing

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 33 | ARCH-004, ARCH-005, ARCH-008, ARCH-010, ARCH-011, ARCH-013, ARCH-018, ARCH-022 | PASSED | Extend the authoritative OpenAPI contract and both backend operations with the ISSUE-010 quantity-editor data. Add one required closed `allowedQuantities` array to every suggestion item. It contains one or two unique closed objects with `unit` and positive whole `maximumValue`; the default unit is first. A Food Object without a Serving exposes only its `g` or `ml` base unit with maximum `100000`. A Food Object with a Serving exposes `serving` first with the maximum equal to the whole-number floor of `100000` divided by its stored Serving base quantity, then its `g` or `ml` base unit with maximum `100000`. Keep the existing `1 serving`, `100 g`, and `100 ml` defaults, and do not expose Physical State or the stored Serving quantity. Add required `inputMacronutrients` to every successful Substitute response. Calculate its protein, carbohydrate, and fat for the committed Substitution Input quantity in the Find Substitute Page Module, then apply the existing backend `0.1 g` half-up display projection. Regenerate the Go transport models and TypeScript client and types, keep generated values at the HTTP Adapter, and add no endpoint or browser-side nutrition calculation, following the [Food Quantity editing plan](plan.md#phase-10--food-quantity-editing), [ARCH-004](../architecture/architecture.md#arch-004--suggest-food-objects-module), [ARCH-005](../architecture/architecture.md#arch-005--find-substitute-page-module), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-010](../architecture/architecture.md#arch-010--food-suggestion-collaboration), [ARCH-011](../architecture/architecture.md#arch-011--substitution-search-collaboration), [ARCH-013](../architecture/architecture.md#arch-013--food-catalog-data), [ARCH-018](../architecture/architecture.md#arch-018--nutrition-and-paging-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-023](../requirements/requirements.md#req-023--serving-default), [REQ-024](../requirements/requirements.md#req-024--nutrition-basis-default), [REQ-025](../requirements/requirements.md#req-025--quantity-syntax), [REQ-028](../requirements/requirements.md#req-028--quantity-recalculation), [REQ-039](../requirements/requirements.md#req-039--display-precision), the [Food Quantity glossary contract](../../CONTEXT.md#nutrition), the [OpenAPI contract](../../api/openapi.yaml), ISSUE-005, and ISSUE-010. | 20 | Run `python3 scripts/ci_check.py` from the repository root. Pass: OpenAPI generation is byte-stable and the generated Go and TypeScript values compile with the required closed fields. Extended real-PostgreSQL HTTP integration tests `TestFoodSuggestionsHTTPIntegration` and `TestSubstituteSearchContractHTTPIntegration` prove exact field sets; default-first allowed quantities for seeded solid-Serving, liquid-Serving, solid-base-only, and liquid-base-only Food Objects; `100000` base maxima; floored whole Serving maxima; and `inputMacronutrients` on every successful page. Extended `TestFindSubstitutePageIntegration` proves that `1 serving`, `100 g`, `100 ml`, and changed accepted quantities produce the expected backend-scaled, half-up one-decimal input protein, carbohydrate, and fat without changing ranked result IDs or order. |
| 34 | ARCH-001, ARCH-002, ARCH-003, ARCH-008, ARCH-011, ARCH-018, ARCH-019, ARCH-020, ARCH-022 | PASSED | Implement the ISSUE-010 editable selected-food summary over task 33's generated contract. Extend the single discriminated interaction state with exact raw number text, draft and committed units, the selected `allowedQuantities`, the committed transport quantity, validation state, and current page index while TanStack Query retains response data and pending state. Render one unbordered five-row summary at every supported width: the captured localized food name; the number field with a current-unit-first selector when two units are allowed or static `g` or `ml` text when only one is allowed; then backend-provided protein, carbohydrate, and fat rows using the existing captured-language labels and one-decimal formatting. Keep `Selected food` or `Wybrany produkt`, `Quantity` or `Ilość`, and `Unit` or `Jednostka` as visually hidden accessible text. Label selector options `g`, `ml`, `servings`, or `porcje`. During the initial new Search, show the complete summary with disabled controls and a `16px` spinner in each macronutrient value position, mark the region busy, and politely announce `Loading nutrition values` or `Ładowanie wartości odżywczych`. Use a text input so invalid raw text remains observable. Accept base-unit text only when it starts with an ASCII digit from 1 through 9 and continues with zero or more ASCII digits. Accept Serving text in that integer form or with a canonical integer part, one dot, and one or more ASCII fractional digits, with a numeric value greater than zero; permit trailing fractional zeros. Reject surrounding whitespace, a leading plus sign, leading zeros, exponent notation, `.5`, `1.`, comma decimals, zero, negatives, empty text, and letters. Enter or focus leaving the complete quantity editor commits the number; moving focus inside the editor does not commit an old unit before a selector change. A unit selection replaces the draft with `1` for Serving or `100` for a base unit and commits immediately. A syntactically valid number above the selected `maximumValue` is silently replaced by that whole maximum before commit, with no visible or assistive clamp notice. A valid draft that resolves to the committed numeric value clears validation but starts no request, including Enter followed by blur and a clamp back to the committed maximum. Invalid commit keeps the exact text, starts no request, sets `aria-invalid`, and shows `Enter a valid quantity.` or `Wpisz prawidłową ilość.` through an associated polite live message; natural focus is unchanged, and the error clears as soon as the draft becomes syntactically valid. Each changed valid commit clears the error and starts one fresh generated-client Substitute request with the same Food Object ID and current page. While the current recalculation is pending, keep controls enabled; retain names, images, labels, and quantity-independent similarity; replace every selected-input macronutrient and every card Matched Quantity, protein, carbohydrate, and fat value with an aria-hidden `16px` spinner; mark the combined region busy; and politely announce `Updating quantities` or `Aktualizowanie ilości` once. Render only the current response's backend values and preserve its page, Food Object IDs, and order. Add no browser nutrition calculation, response-data store, MORE!, Phase 12 global request-lock completion, request-failure state, card motion, or active-content language change, following the [Food Quantity editing plan](plan.md#phase-10--food-quantity-editing), [ARCH-001](../architecture/architecture.md#arch-001--browser-application), [ARCH-002](../architecture/architecture.md#arch-002--browser-interaction-module), [ARCH-003](../architecture/architecture.md#arch-003--translation-module), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [ARCH-011](../architecture/architecture.md#arch-011--substitution-search-collaboration), [ARCH-018](../architecture/architecture.md#arch-018--nutrition-and-paging-mechanism), [ARCH-019](../architecture/architecture.md#arch-019--request-control-and-failure-mechanism), [ARCH-020](../architecture/architecture.md#arch-020--responsive-accessible-presentation-mechanism), [ARCH-022](../architecture/architecture.md#arch-022--architecture-verification-mechanism), [REQ-025](../requirements/requirements.md#req-025--quantity-syntax), [REQ-026](../requirements/requirements.md#req-026--invalid-quantity), [REQ-027](../requirements/requirements.md#req-027--quantity-editing), [REQ-028](../requirements/requirements.md#req-028--quantity-recalculation), the [Food Quantity glossary contract](../../CONTEXT.md#nutrition), the [style and accessibility specification](../requirements/style.md), and ISSUE-010. | 31, 33 | Run `bun run typecheck`, `bun test`, and `bun run test:e2e` from `frontend/` (`P10-G1`). Pass: new real-stack Playwright scenario `food-quantity-editing.spec.ts` selects seeded solid-Serving, liquid-Serving, solid-base-only, and liquid-base-only inputs; observes the complete disabled initial summary, three compact spinners, and localized loading status; then edits the enabled quantity after the first result page loads (`P10-G2`, `REQ-027`). It proves default-first and current-first two-unit selector order, static single base units, plural Serving labels, `1` and `100` unit-reset commits, maximum clamping without a clamp notice, and no request when the clamped or directly entered numeric value equals the committed value. It accepts positive base-unit integers and dot-decimal Serving counts (`P10-G3`, `REQ-025`) and rejects fractional base values, comma decimals, zero, negatives, empty text, letters, and every other ISSUE-010-rejected form (`P10-G4`). Every invalid case retains exact raw text, natural focus, and the exact associated English or Polish polite message until the draft becomes valid (`P10-G5`, `REQ-026`), while the request observer records zero additional Substitute POSTs (`P10-G6`). A delayed valid recalculation leaves controls enabled, exposes one localized busy status, and puts aria-hidden `16px` spinners in every quantity-dependent selected-food and card value while keeping names, images, labels, and similarity visible. Each changed commit records one request with the selected Food Object ID, selected unit, committed or clamped number, and unchanged current page; the response and rendered summary and cards show expected proportional Matched Quantity and macronutrient changes (`P10-G7`, `REQ-028`). The response page index and ordered Food Object IDs equal the first page, and rendered card-name order matches those IDs (`P10-G8`). Existing pointer, keyboard, and result-state scenarios pass unchanged. |

## Phase 19 — Compact calorie result presentation

| ID | Architecture Component | Status | Description | Depends On (ID) | Verification Criteria |
| --- | --- | --- | --- | --- | --- |
| 35 | ARCH-001, ARCH-003, ARCH-005, ARCH-008, ARCH-011, ARCH-018, ARCH-020, ARCH-022 | PASSED | Extend the Substitute response with required backend-derived whole `inputCalories` and item `calories` fields. Calculate calories from full-precision scaled Macro Profiles with `4 × protein + 4 × carbohydrate + 9 × fat`, then round only in the Go display projection. Regenerate the Go and TypeScript transports. Replace the selected-food summary with one compact centered card that keeps the existing quantity editor; add localized `Calories` or `Kalorie` rows with `kcal` for the input and every result card; and add the centered localized `Found substitutions` or `Znalezione zamienniki` heading above the result grid. Keep the 1920 × 1080 three-card first-result layout free from vertical scrolling. Do not calculate or round calories in the browser. Follow the [compact calorie result presentation plan](plan.md#phase-19--compact-calorie-result-presentation), [ARCH-005](../architecture/architecture.md#arch-005--find-substitute-page-module), [ARCH-008](../architecture/architecture.md#arch-008--openapi-http-interface), [REQ-078](../requirements/requirements.md#req-078--displayed-calories), and [REQ-079](../requirements/requirements.md#req-079--compact-result-presentation). | 34 | Run `go test ./...` from `backend/` (`P19-G1`). Run `bun run typecheck`, `bun run format:check`, and `bun run test:e2e` from `frontend/` (`P19-G2`). Pass: closed API responses contain exact backend-projected whole calorie fields; English and Polish selected-food and result cards show those values with localized labels and `kcal`; quantity editing remains usable (`P19-G3`). At 1920 × 1080, execute a three-card result search and verify the input card, centered heading, and all cards are visible without vertical scroll (`P19-G4`). Verify the input and every card show the API calorie values with `Calories` or `Kalorie` and `kcal` (`P19-G5`). |
