# Backlog

Whatever ideas that are not right now in the plan, but might be implemented later.

## IDEA-001

Currently the weight/portion updates are done in the backend.

Maybe the better way would be to put it in the frontend, handled by the browser?

This way we do not need to make a new request and the update should be faster.

## IDEA-002

Animation should block the MORE! button until it finishes.

This should be done when we do stylistic revamp.

## IDEA-003

Currently, the website is a bit... uninspiring for cooking.

It works, but it could use some stylistic sugar.

We have cards now, which works. I think they can be improved. Maybe style the cards as tables? Foods served as animation? then the macros text shows up.

I was wondering if adding some kind of a cat type chef waiting for prompt would make any sense.

Also, the website misses some kind of welcome text.

## IDEA-004

We can improve the product by enabling us to change not only weight, but also calories and macros. this way if someone is looking for a meal and notices 'hey, I would like to find out what is the portion for 20 g of protein' then they just edit it and get the answer. We need to get rid of input boxes. They work, but look sad. uninspiring.

## IDEA-005

When user clicks on a card, they can be taken to a separate page with a recipe for the meal. I am not a cook, no idea how to; but we can find some of them for sure in the internet and reference them at the top. We will not copy the recipe, that's bad; instead, we will provide a chart - see `docs/requirements/cooking-chart.jpeg`

## IDEA-006

Sometimes, someone wants to simply have an idea for a meal, not to search for replacement. When page-per-meal is implemented, we can add a 'feeling lucky' type of button a'la Google that takes the user to the meal page immediately. there we can add an option to allow to search for replacement for that meal.
